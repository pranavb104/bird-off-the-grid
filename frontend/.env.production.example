# Production — served via nginx on port 80.
# Relative URL so axios uses the same origin; nginx proxies to backend.
VUE_APP_API_URL=/api
# WebSocket: leave empty; App.vue derives ws://HOST/ws at runtime.
VUE_APP_WS_URL=
